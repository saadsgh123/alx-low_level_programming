/**
 * main - Prints all single digit numbers of base 10 starting from 0
 * followed by a new line
 *
 * Return: Always 0.
 */
#include <stdio.h>
int main(void)
{
char i;
for (i = 48; i < 58; i++)
putchar(i);

printf("\n");
return (0);
}
