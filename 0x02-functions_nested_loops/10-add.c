/**
 * add - function that add two numbers
 * and returns the result.
 * @a: first para..
 * @b: second para.
 * Return: Always add result.
 */
#include "main.h"
int add(int a, int b)
{
int r = a + b;
return (r);
}
